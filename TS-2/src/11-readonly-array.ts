// const numbers: number[] = [1,2,2,3]
const numbers: ReadonlyArray<number>= [1,2,2,3]


// //Insert at the end
// numbers.push(9)
// //Delete the last element
// numbers.pop()
// //Insert in the beginning of the array
// numbers.unshift(1)

numbers.filter(()=>{})
numbers.reduce(()=> 0)
numbers.map(()=>{})
